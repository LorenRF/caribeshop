{
  "Logging": {
    "LogLevel": {
      "Default": "Information", // Nivel de logs por defecto
      "Microsoft.AspNetCore": "Warning" // Nivel de logs para Microsoft.AspNetCore
    }
  },

  "ConnectionStrings": {
    "DB": "Server=LORENSLAPTOP;Database=CaribeShop;Trusted_connection=true;TrustServerCertificate=true;MultipleActiveResultSets=True;"
    // Cadena de conexión a la base de datos CaribeShop en el servidor local
  },

  "JwtSettings": {
    "SecretKey": "X9v!7kLm#Qp2&8RfZtWx3@NcB4EjHsYD", // Clave secreta para generación de JWT
    "Issuer": "CaribeShop", // Emisor del token JWT
    "Audience": "CaribeShopUser", // Audiencia del token JWT
    "ExpirationMinutes": 60 // Tiempo de expiración en minutos del token JWT
  },

  "swagger": "2.0", // Versión Swagger para documentación API
  "openapi": "3.1.0", // Versión OpenAPI para documentación API

  "AllowedHosts": "*" // Hosts permitidos para la aplicación
}
